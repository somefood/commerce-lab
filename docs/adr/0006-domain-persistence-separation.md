# ADR-0006: 도메인 모델과 영속성 모델을 분리한다

## 상태

채택 (2026-08-2?)  <!-- 실제 합의한 날짜로 고칠 것 -->

## 배경

<!-- 사용자가 채운다: 어떤 문제 상황이었는지
     - Order(도메인)와 OrderEntity(JPA)를 하나로 합치면 뭐가 편한가? 그게 왜 문제인가?
     - JPA가 엔티티에 요구하는 것들(기본 생성자, var, 프록시)이
       도메인 설계에 어떤 제약을 만드나? -->

## 결정

<!-- 사용자가 채운다: 무엇을 하기로 했는지
     함께 정한 것: 도메인은 domain 패키지, 엔티티는 infrastructure 패키지.
     ArchUnit이 이 경계를 강제한다 (`order 도메인은 영속성 기술을 알지 못한다`) -->

## 대안

<!-- 사용자가 채운다: 무엇을 버렸고 왜
     후보였던 것: 도메인 클래스에 @Entity를 직접 붙이기 (가장 흔한 방식이다)
     그게 왜 이 프로젝트에서는 안 되나? 3단계 조건부 UPDATE와 어떻게 부딪히나? -->

## 결과

<!-- 사용자가 채운다: 무엇을 얻고 무엇을 포기했는지
     포기한 쪽이 명확한 결정이다 — 매핑 코드를 손으로 써야 한다.
     그 비용이 실제로 얼마나 됐나? (toDomain() 함수들을 세어볼 것)
     그만한 값어치가 있었나? 지금 시점에서 솔직하게 -->

## 관련

- 설계 근거: [M1 §4-1](../milestones/M1-order-core.md)
- 강제 장치: `bootstrap/src/test/.../ArchitectureTest.kt`
- 매핑 코드: `order-core/.../infrastructure/*Adapter.kt`

---

**작성 안내:** 이 ADR은 사용자가 직접 채운다. Claude가 설명한 설계 의도를
자기 언어로 옮겨 적는 것이 이해했다는 증거이며, 면접 답변의 원본이 된다.
