# ADR-0004: 선점 만료를 주기 배치 단독으로 구동한다

## 상태

채택 (2026-08-2?)  <!-- 실제 합의한 날짜로 고칠 것 -->

## 배경

<!-- 사용자가 채운다: 어떤 문제 상황이었는지
     - 선점(HELD)에 TTL이 있다. 시간이 지나면 재고를 돌려줘야 한다.
     - "시간이 지났다"는 것을 누가 알아차리나? 아무도 요청을 보내지 않아도 만료돼야 하나? -->

## 결정

<!-- 사용자가 채운다: 무엇을 하기로 했는지
     함께 정한 수치: TTL 3분 / 주기 5초 / 1회 500건
     이 숫자들이 왜 이 값인지도 쓸 것. 근거 없이 고른 숫자면 "임의로 정했다"고 쓸 것 -->

## 대안

<!-- 사용자가 채운다: 무엇을 버렸고 왜
     후보였던 것: Redis TTL 키 만료 이벤트 / DB 조회 시점에 lazy 만료 / 배치 + Redis 병행
     특히 Redis를 왜 M1에서 안 쓰기로 했는지 —
     "선점 만료의 진실의 원천은 DB"라는 불변 규칙(CLAUDE.md §5)과 어떻게 이어지나? -->

## 결과

<!-- 사용자가 채운다: 무엇을 얻고 무엇을 포기했는지
     포기한 것 예시: 만료가 즉시가 아니라 최대 5초 늦다.
     그 지연이 사용자에게 어떻게 보이나? 감수할 만한가? -->

## 관련

- 작업지시서: [M1 §11](../milestones/M1-order-core.md)
- 3단계에서 이 결정이 코드가 된다. 그때 이 문서를 다시 열 것

---

**작성 안내:** 이 ADR은 사용자가 직접 채운다. Claude가 설명한 설계 의도를
자기 언어로 옮겨 적는 것이 이해했다는 증거이며, 면접 답변의 원본이 된다.
